function Table({ data }) {  const headerList = data.header.map((title, index) => <th key={`${title}-${index}`}>{title}</th>)  return (    <>      <table>        <thead>          <tr>{ headerList }</tr>        </thead>        <tbody>          {            data.rows.map((row, index) =>            <tr key={index}>              {row.map((col, index) => <td key={index}>{col}</td>)}            </tr>)          }        </tbody>      </table>      <pre>{JSON.stringify(data.rows)}</pre>    </>  )}export default Table